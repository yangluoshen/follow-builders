# Univer Workbook Visual Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make each Univer weekly sheet render as a compact Editorial Dashboard with KPI cards and a readable formula-driven digest table.

**Architecture:** Keep `raw-data` and `runs` as the stable data layer. Update the workbook-local script generated by `scripts/update-univer-workbook.js` so weekly sheets are deterministically redrawn from sorted `raw-data` row references. Rebuild `templates/follow-builders.univer` from `scripts/univer-template-scaffold.js` so new users initialize with the same compact visual contract.

**Confirmed second visual iteration:** Remove the old Top X / Top Podcast / Highest Score highlight band. Weekly sheets use row 1 title, row 2 metadata, row 3 KPI labels, rows 4-5 KPI formulas, row 6 Daily Digest title, row 7 table header, and rows 8+ formula/reference rows into `raw-data`. Freeze rows is 7 and freeze columns is 0.

**Tech Stack:** Node.js ESM, `node:test`, Univer CLI `.univer` workbooks, workbook-local `univer run` scripts, `univer inspect`, `univer view`.

---

## File Structure

- Modify `scripts/update-univer-workbook.js`
  - Add dashboard metric formula helpers inside `buildWorkbookRunScript`.
  - Replace the current plain `renderWeeklySheet` with the compact Editorial Dashboard renderer.
  - Preserve sorted `raw-data` row numbers and write weekly data cells as formulas/references into `raw-data`.
  - Keep raw-data upsert, runs append, sorting, commit, and sync behavior unchanged.
- Modify `scripts/update-univer-workbook.test.js`
  - Extend the fake workbook/range to record formatting calls needed by tests.
  - Add tests for dashboard KPI formulas, no highlight labels, readable table row placement, raw-data formula references, and deterministic clearing.
- Modify `scripts/univer-template-scaffold.js`
  - Update `_week-template` scaffold to match the dashboard shell and table layout.
  - Keep template raw/runs headers unchanged.
- Modify `templates/follow-builders.univer`
  - Regenerate via `univer new`, `univer run --file scripts/univer-template-scaffold.js`, and `univer commit`.
- Modify `SKILL.md`
  - Document the Editorial Dashboard layout contract.
- No changes to raw-data schema, Telegram delivery, `.xlsx` export, or remote sync semantics.

## Task 1: Add Tests For Dashboard Rendering Contract

**Files:**
- Modify: `scripts/update-univer-workbook.test.js`

- [ ] **Step 1: Extend `FakeRange` formatting methods so tests can assert visible styles**

Add this storage to the `FakeSheet` constructor:

```js
this.formatting = [];
this.merges = [];
this.clearedRanges = [];
this.columnWidths = new Map();
this.rowHeights = new Map();
this.frozenRows = 0;
this.frozenColumns = 0;
this.hiddenGridlines = false;
```

Replace the no-op formatting methods in `FakeRange` with recorders:

```js
record(method, value) {
  this.sheet.formatting.push({
    method,
    value,
    row: this.row,
    column: this.column,
    rowCount: this.rowCount,
    columnCount: this.columnCount
  });
  return this;
}

clear() {
  this.clearContent();
  this.sheet.clearedRanges.push({
    row: this.row,
    column: this.column,
    rowCount: this.rowCount,
    columnCount: this.columnCount
  });
  return this;
}

merge(options = {}) {
  this.sheet.merges.push({
    row: this.row,
    column: this.column,
    rowCount: this.rowCount,
    columnCount: this.columnCount,
    options
  });
  return this;
}

setFontWeight(value) { return this.record('setFontWeight', value); }
setBackgroundColor(value) { return this.record('setBackgroundColor', value); }
setFontColor(value) { return this.record('setFontColor', value); }
setVerticalAlignment(value) { return this.record('setVerticalAlignment', value); }
setFontSize(value) { return this.record('setFontSize', value); }
setHorizontalAlignment(value) { return this.record('setHorizontalAlignment', value); }
setWrap(value) { return this.record('setWrap', value); }
```

Replace the no-op sheet layout methods in `FakeSheet` with recorders:

```js
setFrozenRows(value) { this.frozenRows = value; return this; }
setFrozenColumns(value) { this.frozenColumns = value; return this; }
setHiddenGridlines(value) { this.hiddenGridlines = value; return this; }
setColumnWidths(start, count, width) {
  for (let index = 0; index < count; index += 1) this.columnWidths.set(start + index, width);
  return this;
}
setColumnWidth(index, width) { this.columnWidths.set(index, width); return this; }
setRowHeights(start, count, height) {
  for (let index = 0; index < count; index += 1) this.rowHeights.set(start + index, height);
  return this;
}
setRowHeight(index, height) { this.rowHeights.set(index, height); return this; }
```

- [ ] **Step 2: Add a compact dashboard contract test**

Append or update a test after `generated workbook-local script renders the weekly sheet from raw-data history` to assert:

- `frozenRows` is `7` and `frozenColumns` is `0`.
- KPI cards in rows 4-5 contain `COUNTIFS` / `AVERAGEIFS` formulas against `raw-data`.
- Row 6 contains `Daily Digest`, row 7 contains the table header, and rows 8+ contain formulas such as `='raw-data'!J2`.
- Old highlight labels are absent.

- [ ] **Step 3: Run the focused test and confirm it fails**

Run:

```bash
cd scripts && node --test update-univer-workbook.test.js
```

Expected: FAIL because the old renderer freezes columns, writes literal weekly rows, and keeps the old highlight band instead of the compact row 6/7 layout.

- [ ] **Step 4: Commit the failing test**

```bash
git add scripts/update-univer-workbook.test.js
git commit -m "test: cover univer editorial dashboard rendering"
```

## Task 2: Implement Editorial Dashboard Weekly Renderer

**Files:**
- Modify: `scripts/update-univer-workbook.js`
- Test: `scripts/update-univer-workbook.test.js`

- [ ] **Step 1: Add dashboard constants inside the generated workbook script**

Inside `buildWorkbookRunScript`, replace these generated-script constants:

```js
const DISPLAY_HEADER_ROW = 6;
const DISPLAY_DATA_ROW = DISPLAY_HEADER_ROW + 1;
```

with:

```js
const DISPLAY_HEADER_ROW = 6;
const DISPLAY_DATA_ROW = DISPLAY_HEADER_ROW + 1;
const DASHBOARD_CLEAR_ROWS = 14;
const TABLE_CLEAR_EXTRA_ROWS = 160;
const COLORS = {
  title: '#102033',
  titleSoft: '#1E3A5F',
  x: '#2563EB',
  podcast: '#7C3AED',
  blog: '#F59E0B',
  green: '#16A34A',
  greenSoft: '#DCFCE7',
  yellowSoft: '#FEF3C7',
  redSoft: '#FEE2E2',
  sheet: '#F6F8FB',
  card: '#FFFFFF',
  border: '#E2E8F0',
  text: '#111827',
  muted: '#64748B',
  tableHeader: '#1F4E79',
  tableAlt: '#F8FBFF'
};
```

- [ ] **Step 2: Add helper functions before `renderWeeklySheet`**

Insert these helpers after `buildWeeklyRowsFromRaw`:

```js
function sourceTypeFromDisplayType(value) {
  const normalized = stringValue(value).toLowerCase();
  if (normalized === 'x') return 'x';
  if (normalized === 'podcast') return 'podcast';
  if (normalized === 'blog') return 'blog';
  return normalized;
}

function shortDate(dateText) {
  const parts = stringValue(dateText).split('-');
  if (parts.length !== 3) return stringValue(dateText);
  const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
  const month = months[Number(parts[1]) - 1] || parts[1];
  return month + ' ' + Number(parts[2]);
}

function humanDateRange() {
  if (!payload.weekStartDate || !payload.weekEndDate) return payload.sheetNames.week;
  return shortDate(payload.weekStartDate) + ' - ' + shortDate(payload.weekEndDate);
}

function truncateText(value, maxLength) {
  const text = stringValue(value).replace(/\\s+/g, ' ').trim();
  if (text.length <= maxLength) return text;
  return text.slice(0, maxLength - 1) + '…';
}

function countBySource(rows) {
  const counts = { x: 0, podcast: 0, blog: 0 };
  rows.forEach(row => {
    const source = sourceTypeFromDisplayType(row[1]);
    if (Object.prototype.hasOwnProperty.call(counts, source)) counts[source] += 1;
  });
  return counts;
}

function scoreFill(score) {
  const value = Number(score);
  if (!Number.isFinite(value)) return COLORS.card;
  if (value >= 85) return COLORS.greenSoft;
  if (value >= 60) return COLORS.yellowSoft;
  return COLORS.redSoft;
}

function sourceAccent(displayType) {
  const source = sourceTypeFromDisplayType(displayType);
  if (source === 'x') return COLORS.x;
  if (source === 'podcast') return COLORS.podcast;
  if (source === 'blog') return COLORS.blog;
  return COLORS.muted;
}

function applyRangeBox(range, backgroundColor, fontColor) {
  range
    .setBackgroundColor(backgroundColor)
    .setFontColor(fontColor)
    .setVerticalAlignment('middle')
    .setHorizontalAlignment('left');
}
```

- [ ] **Step 3: Replace `renderWeeklySheet` with the dashboard renderer**

Replace the whole current `renderWeeklySheet` function with:

```js
function renderWeeklySheet(sheet, inserted, updated, rows) {
  const headers = payload.weekHeaders;
  const counts = countBySource(rows);
  sheet.setHiddenGridlines(true);
  sheet.setFrozenRows(7);
  sheet.setFrozenColumns(0);

  sheet.getRange(0, 0, DASHBOARD_CLEAR_ROWS, headers.length).clear();
  const clearRows = Math.max(sheet.getLastRow() - DISPLAY_DATA_ROW + 1, rows.length + TABLE_CLEAR_EXTRA_ROWS, 1);
  sheet.getRange(DISPLAY_DATA_ROW, 0, clearRows, headers.length).clearContent();

  sheet.getRange('A1:J1').merge({ isForceMerge: true });
  sheet.getRange('A1').setValue(payload.sheetNames.week + ' Follow Builders');
  applyRangeBox(sheet.getRange('A1:J1'), COLORS.title, '#FFFFFF');
  sheet.getRange('A1:J1').setFontWeight('bold').setFontSize(22);

  sheet.getRange('A2:J2').merge({ isForceMerge: true });
  sheet.getRange('A2').setValue(humanDateRange() + ' · Generated ' + payload.runRecord.finishedAt + ' · ' + (payload.runRecord.publicUrl || 'Local workbook'));
  sheet.getRange('A2:J2').setBackgroundColor('#EAF2F8').setFontColor(COLORS.text).setVerticalAlignment('middle');

  sheet.getRange('A4:B5').merge({ isForceMerge: true }).setValue(rows.length);
  sheet.getRange('C4:D5').merge({ isForceMerge: true }).setValue(counts.x);
  sheet.getRange('E4:F5').merge({ isForceMerge: true }).setValue(counts.podcast);
  sheet.getRange('G4:H5').merge({ isForceMerge: true }).setValue(counts.blog);
  sheet.getRange('I4:J5').merge({ isForceMerge: true }).setValue(averageScore === '' ? '—' : averageScore);
  [
    ['A4:B5', COLORS.titleSoft],
    ['C4:D5', COLORS.x],
    ['E4:F5', COLORS.podcast],
    ['G4:H5', COLORS.blog],
    ['I4:J5', COLORS.green]
  ].forEach(([a1, color]) => {
    sheet.getRange(a1).setBackgroundColor(color).setFontColor('#FFFFFF').setFontWeight('bold').setFontSize(18).setHorizontalAlignment('center').setVerticalAlignment('middle');
  });
  sheet.getRange('A3').setValue('Items');
  sheet.getRange('C3').setValue('X');
  sheet.getRange('E3').setValue('Podcast');
  sheet.getRange('G3').setValue('Blog');
  sheet.getRange('I3').setValue('Avg Score');

  sheet.getRange('A6:J6').merge({ isForceMerge: true }).setValue('Daily Digest');
  sheet.getRange('A6:J6').setBackgroundColor(COLORS.sheet).setFontColor(COLORS.text).setFontWeight('bold').setFontSize(16).setVerticalAlignment('middle');

  sheet.getRange(DISPLAY_HEADER_ROW, 0, 1, headers.length).setValues([headers]);
  sheet
    .getRange(DISPLAY_HEADER_ROW, 0, 1, headers.length)
    .setFontWeight('bold')
    .setFontColor('#FFFFFF')
    .setBackgroundColor(COLORS.tableHeader)
    .setVerticalAlignment('middle')
    .setHorizontalAlignment('left');

  if (rows.length > 0) {
    sheet.getRange(DISPLAY_DATA_ROW, 0, rows.length, headers.length).setValues(rows);
    sheet.getRange(DISPLAY_DATA_ROW, 0, rows.length, headers.length).setVerticalAlignment('top').setHorizontalAlignment('left').setWrap(true);
    sheet.setRowHeights(DISPLAY_DATA_ROW, rows.length, 96);
    rows.forEach((row, index) => {
      const targetRow = DISPLAY_DATA_ROW + index;
      const rowFill = index % 2 === 0 ? COLORS.tableAlt : COLORS.card;
      sheet.getRange(targetRow, 0, 1, headers.length).setBackgroundColor(rowFill);
      sheet.getRange(targetRow, 1).setFontColor(sourceAccent(row[1])).setFontWeight('bold');
      sheet.getRange(targetRow, 7).setBackgroundColor(scoreFill(row[7])).setFontWeight('bold').setHorizontalAlignment('center');
    });
  }

  const widths = [104, 88, 150, 300, 430, 360, 170, 86, 330, 190];
  widths.forEach((width, index) => sheet.setColumnWidth(index, width));
  sheet.setRowHeight(0, 34);
  sheet.setRowHeight(1, 28);
  sheet.setRowHeight(2, 26);
  sheet.setRowHeight(3, 44);
  sheet.setRowHeight(4, 44);
  sheet.setRowHeight(6, 24);
  sheet.setRowHeight(7, 34);
  sheet.setRowHeight(8, 46);
  sheet.setRowHeight(9, 46);
  sheet.setRowHeight(11, 30);
  sheet.setRowHeight(12, 30);
  sheet.setRowHeight(DISPLAY_HEADER_ROW, 32);
}
```

Update `FakeSheet.getRange` to support the A1 string ranges used by the renderer. Use this small parser:

```js
function a1ToIndexes(a1) {
  const match = /^([A-Z]+)(\d+)(?::([A-Z]+)(\d+))?$/.exec(a1);
  if (!match) throw new Error(`Unsupported fake A1 range: ${a1}`);
  const col = text => text.split('').reduce((sum, char) => sum * 26 + char.charCodeAt(0) - 64, 0) - 1;
  const startColumn = col(match[1]);
  const startRow = Number(match[2]) - 1;
  const endColumn = match[3] ? col(match[3]) : startColumn;
  const endRow = match[4] ? Number(match[4]) - 1 : startRow;
  return { row: startRow, column: startColumn, rowCount: endRow - startRow + 1, columnCount: endColumn - startColumn + 1 };
}
```

and at the start of `FakeSheet.getRange`:

```js
if (typeof row === 'string') {
  const parsed = a1ToIndexes(row);
  return new FakeRange(this, parsed.row, parsed.column, parsed.rowCount, parsed.columnCount);
}
```

- [ ] **Step 4: Run focused tests**

Run:

```bash
cd scripts && node --test update-univer-workbook.test.js
```

Expected: PASS.

- [ ] **Step 5: Run all script tests**

Run:

```bash
cd scripts && npm test
```

Expected: all tests pass.

- [ ] **Step 6: Commit renderer implementation**

```bash
git add scripts/update-univer-workbook.js scripts/update-univer-workbook.test.js
git commit -m "feat: render univer weekly dashboard"
```

## Task 3: Rebuild The Workbook Template Scaffold

**Files:**
- Modify: `scripts/univer-template-scaffold.js`
- Modify: `templates/follow-builders.univer`

- [ ] **Step 1: Update scaffold constants and visual system**

In `scripts/univer-template-scaffold.js`, keep header arrays unchanged. Add these constants after `WEEK_DATA_START_ROW`:

```js
const COLORS = {
  title: '#102033',
  titleSoft: '#1E3A5F',
  x: '#2563EB',
  podcast: '#7C3AED',
  blog: '#F59E0B',
  green: '#16A34A',
  greenSoft: '#DCFCE7',
  yellowSoft: '#FEF3C7',
  redSoft: '#FEE2E2',
  sheet: '#F6F8FB',
  card: '#FFFFFF',
  border: '#E2E8F0',
  text: '#111827',
  muted: '#64748B',
  tableHeader: '#1F4E79',
  tableAlt: '#F8FBFF'
};
```

- [ ] **Step 2: Replace `applyWeekTemplate`**

Replace the current `applyWeekTemplate` function with a template version of the dashboard renderer:

```js
function applyWeekTemplate(sheet) {
  clearScaffoldArea(sheet, 240, WEEK_DISPLAY_HEADERS.length);
  sheet.setHiddenGridlines(true);
  sheet.setFrozenRows(7);
  sheet.setFrozenColumns(0);

  sheet.getRange('A1:J1').merge({ isForceMerge: true });
  sheet.getRange('A1').setValue('Follow Builders Weekly Digest');
  sheet.getRange('A1:J1').setBackgroundColor(COLORS.title).setFontColor('#FFFFFF').setFontWeight('bold').setFontSize(22).setVerticalAlignment('middle');

  sheet.getRange('A2:J2').merge({ isForceMerge: true }).setValue('Week range · Generated timestamp · Public workbook URL');
  sheet.getRange('A2:J2').setBackgroundColor('#EAF2F8').setFontColor(COLORS.text).setVerticalAlignment('middle');

  sheet.getRange('A3').setValue('Items');
  sheet.getRange('C3').setValue('X');
  sheet.getRange('E3').setValue('Podcast');
  sheet.getRange('G3').setValue('Blog');
  sheet.getRange('I3').setValue('Avg Score');
  sheet.getRange('A4:B5').merge({ isForceMerge: true }).setValue("=COUNTIFS('raw-data'!J:J,\">=<weekStart>\",'raw-data'!J:J,\"<=<weekEnd>\")");
  sheet.getRange('C4:D5').merge({ isForceMerge: true }).setValue("=COUNTIFS('raw-data'!J:J,\">=<weekStart>\",'raw-data'!J:J,\"<=<weekEnd>\",'raw-data'!B:B,\"x\")");
  sheet.getRange('E4:F5').merge({ isForceMerge: true }).setValue("=COUNTIFS('raw-data'!J:J,\">=<weekStart>\",'raw-data'!J:J,\"<=<weekEnd>\",'raw-data'!B:B,\"podcast\")");
  sheet.getRange('G4:H5').merge({ isForceMerge: true }).setValue("=COUNTIFS('raw-data'!J:J,\">=<weekStart>\",'raw-data'!J:J,\"<=<weekEnd>\",'raw-data'!B:B,\"blog\")");
  sheet.getRange('I4:J5').merge({ isForceMerge: true }).setValue("=IFERROR(ROUND(AVERAGEIFS('raw-data'!O:O,'raw-data'!J:J,\">=<weekStart>\",'raw-data'!J:J,\"<=<weekEnd>\"),0),\"—\")");
  [
    ['A4:B5', COLORS.titleSoft],
    ['C4:D5', COLORS.x],
    ['E4:F5', COLORS.podcast],
    ['G4:H5', COLORS.blog],
    ['I4:J5', COLORS.green]
  ].forEach(([a1, color]) => {
    sheet.getRange(a1).setBackgroundColor(color).setFontColor('#FFFFFF').setFontWeight('bold').setFontSize(18).setHorizontalAlignment('center').setVerticalAlignment('middle');
  });

  sheet.getRange('A6:J6').merge({ isForceMerge: true }).setValue('Daily Digest');

  sheet.getRange(WEEK_HEADER_ROW, 0, 1, WEEK_DISPLAY_HEADERS.length).setValues([WEEK_DISPLAY_HEADERS]);
  styleHeader(sheet.getRange(WEEK_HEADER_ROW, 0, 1, WEEK_DISPLAY_HEADERS.length), COLORS.tableHeader);

  const widths = [104, 88, 150, 300, 430, 360, 170, 86, 330, 190];
  widths.forEach((width, index) => sheet.setColumnWidth(index, width));
  sheet.setRowHeight(0, 34);
  sheet.setRowHeight(1, 28);
  sheet.setRowHeight(2, 26);
  sheet.setRowHeight(3, 44);
  sheet.setRowHeight(4, 44);
  sheet.setRowHeight(6, 24);
  sheet.setRowHeight(7, 34);
  sheet.setRowHeight(8, 46);
  sheet.setRowHeight(9, 46);
  sheet.setRowHeight(11, 30);
  sheet.setRowHeight(12, 30);
  sheet.setRowHeight(WEEK_HEADER_ROW, 32);
  sheet.setRowHeights(WEEK_DATA_START_ROW, 80, 96);
  sheet.getRange(WEEK_DATA_START_ROW, 0, 80, WEEK_DISPLAY_HEADERS.length).setVerticalAlignment('top').setWrap(true);

  const scoreRange = sheet.getRange('H16:H200');
  const rule = sheet
    .newConditionalFormattingRule()
    .setColorScale([
      { index: 0, color: '#F8696B', value: { type: univerAPI.Enum.ConditionFormatValueTypeEnum.num, value: 0 } },
      { index: 1, color: '#FFEB84', value: { type: univerAPI.Enum.ConditionFormatValueTypeEnum.num, value: 50 } },
      { index: 2, color: '#63BE7B', value: { type: univerAPI.Enum.ConditionFormatValueTypeEnum.num, value: 100 } }
    ])
    .setRanges([scoreRange.getRange()])
    .build();
  sheet.addConditionalFormattingRule(rule);
}
```

- [ ] **Step 3: Rebuild the repo template**

Run:

```bash
rm -rf /tmp/follow-builders-template-refresh.univer
univer new /tmp/follow-builders-template-refresh.univer --name "Follow Builders"
univer run /tmp/follow-builders-template-refresh.univer --file scripts/univer-template-scaffold.js
univer inspect workbook /tmp/follow-builders-template-refresh.univer
univer inspect range /tmp/follow-builders-template-refresh.univer --range '_week-template!A1:J20'
univer commit /tmp/follow-builders-template-refresh.univer --message "Scaffold follow builders workbook"
rm -rf templates/follow-builders.univer
cp -R /tmp/follow-builders-template-refresh.univer templates/follow-builders.univer
```

Expected:

- `univer inspect workbook` shows sheets `raw-data`, `runs`, `_week-template`.
- `_week-template` used range includes the compact dashboard shell and table area.
- `_week-template!A1` displays `Follow Builders Weekly Digest`.
- `_week-template!A6` displays `Daily Digest`.
- Commit succeeds and template remains local-only, not synced.

- [ ] **Step 4: Verify template state**

Run:

```bash
univer inspect workbook templates/follow-builders.univer
univer inspect range templates/follow-builders.univer --range 'raw-data!A1:T1'
univer inspect range templates/follow-builders.univer --range 'runs!A1:M1'
univer inspect range templates/follow-builders.univer --range '_week-template!A1:J20'
```

Expected: all inspect commands succeed, raw/runs headers are unchanged, and `_week-template` shows the dashboard shell.

- [ ] **Step 5: Commit template refresh**

```bash
git add scripts/univer-template-scaffold.js templates/follow-builders.univer
git commit -m "feat: refresh univer workbook template"
```

## Task 4: Document The Editorial Dashboard Contract

**Files:**
- Modify: `SKILL.md`

- [ ] **Step 1: Update `SKILL.md` Univer workbook section**

In `SKILL.md`, under `## Univer Workbook Output`, add or update bullets so the section states:

```md
### Weekly Sheet Presentation Contract

Weekly sheets use the Editorial Dashboard layout:

- Row 1 is the dark title band: `<week> Follow Builders`.
- Row 2 is the metadata line.
- Row 3 contains KPI labels at A/C/E/G/I.
- Rows 4-5 contain KPI cards with formulas for total, X, Podcast, Blog, and Avg Score.
- Row 6 introduces the Daily Digest table.
- Row 7 renders the stable table header.
- Rows 8+ render formula/reference rows into sorted `raw-data` rows, sorted by date desc, then source order `X`, `Podcast`, `Blog`, then published time desc, then score desc.
- Weekly sheets freeze 7 top rows and 0 columns.
- The LLM must not redesign workbook layout during daily runs. It only writes structured item content.
- `raw-data` remains the source of truth and must not be visually optimized at the expense of stable indexing.
```

- [ ] **Step 2: Run a docs grep check**

Run:

```bash
rg -n "Editorial Dashboard|Weekly Sheet Presentation Contract|raw-data.*source of truth" SKILL.md
```

Expected: all three concepts appear.

- [ ] **Step 3: Commit docs**

```bash
git add SKILL.md
git commit -m "docs: document univer dashboard layout"
```

## Task 5: End-To-End Visual Acceptance

**Files:**
- No repo code changes expected unless this task exposes a bug.

- [ ] **Step 1: Run full tests**

Run:

```bash
cd scripts && npm test
```

Expected: all tests pass.

- [ ] **Step 2: Create temp workbook acceptance directory**

Run from repo root:

```bash
TMP_ROOT="$(mktemp -d "${TMPDIR:-/tmp}/follow-builders-visual-acceptance.XXXXXX")"
TMP_HOME="$TMP_ROOT/home"
WB="$TMP_ROOT/follow-builders.univer"
REAL_UNIVER="$(command -v univer)"
mkdir -p "$TMP_HOME/.follow-builders" "$TMP_ROOT/bin"
cp -R templates/follow-builders.univer "$WB"
cat > "$TMP_HOME/.follow-builders/config.json" <<JSON
{
  "language": "zh",
  "frequency": "daily",
  "delivery": { "method": "stdout" },
  "univer": {
    "enabled": true,
    "workbookPath": "$WB",
    "unitId": "visual-acceptance-local",
    "publicUrl": "https://univer.ai/space/sheets/visual-acceptance-local"
  }
}
JSON
cat > "$TMP_ROOT/bin/univer-no-remote-sync" <<SH
#!/bin/sh
if [ "\$1" = "sync" ]; then
  printf '{"success":true,"filePath":"%s","createdRemoteWorkbook":false,"pulledRemoteChangesetCount":0,"pushedLocalChangesetCount":0,"uncommittedMutationCount":0,"status":{"uncommittedMutationCount":0}}\n' "\$2"
  exit 0
fi
exec "$REAL_UNIVER" "\$@"
SH
chmod +x "$TMP_ROOT/bin/univer-no-remote-sync"
printf '%s\n' "$TMP_ROOT"
```

- [ ] **Step 3: Fetch central content and build workbook items**

Run:

```bash
HOME="$TMP_HOME" node scripts/prepare-digest.js > "$TMP_ROOT/prepare-output.json"
export TMP_ROOT
node --input-type=module <<'JS'
import { createHash } from 'crypto';
import { readFile, writeFile } from 'fs/promises';

const root = process.env.TMP_ROOT;
const input = JSON.parse(await readFile(`${root}/prepare-output.json`, 'utf8'));
const generatedAt = input.generatedAt || new Date().toISOString();
const runDate = generatedAt.slice(0, 10);
function clean(value) { return String(value || '').replace(/\s+/g, ' ').trim(); }
function truncate(value, max = 220) {
  const text = clean(value);
  return text.length > max ? `${text.slice(0, max - 1)}…` : text;
}
function titleFromText(text, fallback) {
  const cleaned = clean(text);
  return cleaned ? truncate(cleaned.split(/[。.!?]\s/)[0] || cleaned, 90) : fallback;
}
function score(tweet, base = 52) {
  const raw = base + Math.log10(1 + Number(tweet.likes || 0)) * 18 + Math.log10(1 + Number(tweet.retweets || 0)) * 12 + Math.log10(1 + Number(tweet.replies || 0)) * 8;
  return Math.max(1, Math.min(100, Math.round(raw)));
}
function blogId(url) {
  return `blog:${createHash('sha256').update(url).digest('hex').slice(0, 12)}`;
}
const items = [];
for (const builder of input.x || []) {
  for (const tweet of builder.tweets || []) {
    if (!tweet.id || !tweet.url) continue;
    const excerpt = truncate(tweet.text, 360);
    items.push({
      contentId: `x:${tweet.id}`,
      sourceType: 'x',
      sourceName: 'X',
      authorName: builder.name || builder.handle || 'Unknown builder',
      authorHandle: builder.handle || '',
      title: titleFromText(tweet.text, `${builder.name || builder.handle} tweet`),
      url: tweet.url,
      publishedAt: tweet.createdAt || '',
      capturedAt: generatedAt,
      runDate,
      textExcerpt: excerpt,
      summary: excerpt,
      keyPoints: [excerpt],
      topics: ['AI builders', 'X'],
      importanceScore: score(tweet),
      likes: Number(tweet.likes || 0),
      retweets: Number(tweet.retweets || 0),
      replies: Number(tweet.replies || 0),
      rawSourceKey: tweet.id
    });
  }
}
for (const episode of input.podcasts || []) {
  if (!episode.guid || !episode.url) continue;
  const excerpt = truncate(episode.transcript || episode.title, 520);
  items.push({
    contentId: `podcast:${episode.guid}`,
    sourceType: 'podcast',
    sourceName: episode.name || episode.source || 'Podcast',
    authorName: episode.name || episode.source || 'Podcast',
    authorHandle: '',
    title: episode.title || `${episode.name || 'Podcast'} episode`,
    url: episode.url,
    publishedAt: episode.publishedAt || '',
    capturedAt: generatedAt,
    runDate,
    textExcerpt: excerpt,
    summary: excerpt,
    keyPoints: [excerpt],
    topics: ['AI builders', 'Podcast'],
    importanceScore: 72,
    likes: 0,
    retweets: 0,
    replies: 0,
    rawSourceKey: episode.guid
  });
}
for (const blog of input.blogs || []) {
  const url = blog.url || blog.link;
  if (!url) continue;
  const excerpt = truncate(blog.summary || blog.text || blog.description || blog.title, 420);
  items.push({
    contentId: blog.contentId || blogId(url),
    sourceType: 'blog',
    sourceName: blog.source || blog.name || 'Blog',
    authorName: blog.author || blog.name || '',
    authorHandle: '',
    title: blog.title || titleFromText(excerpt, 'Blog post'),
    url,
    publishedAt: blog.publishedAt || blog.createdAt || '',
    capturedAt: generatedAt,
    runDate,
    textExcerpt: excerpt,
    summary: excerpt,
    keyPoints: [excerpt],
    topics: ['AI builders', 'Blog'],
    importanceScore: 65,
    likes: 0,
    retweets: 0,
    replies: 0,
    rawSourceKey: url
  });
}
await writeFile(`${root}/items.json`, JSON.stringify({ runId: `visual-${generatedAt}`, generatedAt, items, presentationHints: { weeklyThemes: [], highlightContentIds: [] } }, null, 2));
await writeFile(`${root}/digest.md`, `# Visual acceptance\n\nFetched ${items.length} items.\n`);
console.log(JSON.stringify({ items: items.length, generatedAt }, null, 2));
JS
```

- [ ] **Step 4: Populate the workbook without remote sync**

Run:

```bash
node scripts/update-univer-workbook.js \
  --home "$TMP_HOME" \
  --items-json "$TMP_ROOT/items.json" \
  --markdown-path "$TMP_ROOT/digest.md" \
  --univer-path "$TMP_ROOT/bin/univer-no-remote-sync"
```

Expected JSON includes:

- `"status": "ok"`
- `"runResult": { "success": true, ... }`
- `"weeklyRows"` equals the fetched item count after dedupe.

- [ ] **Step 5: Verify visible workbook ranges**

Run:

```bash
univer inspect workbook "$WB"
univer inspect range "$WB" --range 'raw-data!A1:T5'
univer inspect range "$WB" --range 'runs!A1:M3'
univer inspect range "$WB" --range '2026-W22!A1:J20'
```

Expected:

- Workbook contains `raw-data`, `runs`, `_week-template`, and the current ISO week sheet.
- `raw-data` has appended rows.
- `runs` has one run row.
- Week sheet first viewport contains `Follow Builders`, KPI formula cells, `Daily Digest`, the table header, and formula/reference rows into `raw-data`. It does not contain `Top X`, `Top Podcast`, or `Highest Score`.

- [ ] **Step 6: Open `univer view` for human inspection**

Run:

```bash
univer view "$WB" --open --json
```

Expected:

- Browser opens the workbook.
- At 100% zoom, the first viewport looks like the approved Editorial Dashboard direction.
- The digest table rows wrap text and no longer read as clipped single-line raw grid output.

- [ ] **Step 7: Commit any visual acceptance correction**

If visual acceptance reveals a small formatting issue, fix the relevant renderer/template code, run:

```bash
cd scripts && npm test
```

then commit:

```bash
git add scripts/update-univer-workbook.js scripts/update-univer-workbook.test.js scripts/univer-template-scaffold.js templates/follow-builders.univer SKILL.md
git commit -m "fix: polish univer dashboard visual acceptance"
```

If no fixes are needed, do not create an empty commit.

## Self-Review Checklist

- Spec coverage:
  - Editorial Dashboard layout: Tasks 2 and 3.
  - KPI formula cards and raw-data reference rows: Tasks 1, 2, 3.
  - Readable table widths/wrap/row heights: Tasks 1, 2, 3.
  - raw-data source of truth and weekly history: Task 2 keeps existing raw-data flow and tests continue to cover history.
  - Documentation: Task 4.
  - Acceptance with `univer view`: Task 5.
- No raw-data schema changes are planned.
- No `.xlsx` export is planned.
- No real remote sync is required for visual acceptance; Task 5 uses fake sync.
